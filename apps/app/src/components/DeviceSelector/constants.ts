export const PLATFORM_LABELS = {
  ios: 'iOS',
  android: 'Android',
} as const;

export const STATUS_LABELS = {
  connected: 'Connected',
  'device-only': 'Device running',
  offline: 'Offline',
} as const;
